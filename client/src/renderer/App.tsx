export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 32 }}>
      <h1>LOL Helper</h1>
      <p>Main window placeholder — M2 milestone will fill this in.</p>
      <ul>
        <li>Dashboard</li>
        <li>Match history</li>
        <li>Settings (Mayhem upload toggle, hotkeys, language)</li>
        <li>Login (Google OAuth via loopback redirect)</li>
      </ul>
    </main>
  );
}
